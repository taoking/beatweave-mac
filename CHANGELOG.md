# Changelog

所有显著变更都会记录在此文件中。

## [Unreleased]

### Added

- 初始化 Phase 0 原生 macOS 文档应用基础。
- 版本化 `.beatweave` 包、稳定 JSON 编解码与项目包单元测试。
- 增加 GitHub Actions 构建工作流。
- 新增媒体导入、元数据、缩略图、来源状态、重连和 AVPlayer 源媒体预览。
- 新增媒体导入、来源状态与媒体库操作测试。
- 新增可持久化、可再生的多分辨率波形缓存和波形时间线视图。
- 新增 PCM 波形生成及项目包缓存往返测试。
- 新增本地 vDSP FFT 节拍分析、起音/BPM/节拍网格、手动 BPM、半/双拍与 Tap Tempo。
- 新增合成点击音频端到端分析、信号处理与节拍标记持久化测试。
- 新增可撤销的时间线命令层、视频/原始音频剪辑联动、节拍感知吸附、拖放、裁剪、分割、重排与波纹删除。
- 新增时间线插入、移动、裁剪、分割、波纹删除、吸附及撤销/重做测试。
- 新增节拍同步 AutoCut 计划、每 1/2/4/8 拍、素材选择、顺序/确定性打乱、时长与目标范围控制。
- 新增 AutoCut 计划预览摘要和一次性应用/撤销测试。
- 新增本地 H.264/HEVC MP4 导出、分辨率/帧率/质量选项、音频混音、进度、取消和访达定位。
- 新增导出时间规划、H.264+音乐轨、HEVC、尺寸/时长保持及拒绝覆盖现有输出的测试。
- 新增 Phase 7 的可撤销画布、剪辑构图/速度/转场和音频检查器，以及旧项目兼容与编辑命令测试。
- 导出新增画布尺寸同步、适应/填充、裁剪、透明度渐变、主音量、原声静音/音量和音乐淡入淡出处理。
- 新增可选的本地素材质量分析、Vision 人脸信号、运动估计、素材排除/固定优先、强拍模式和自适应 AutoCut 测试。
- 新增项目包缩略图/代理缓存、串行后台生成队列、代理预览与原始媒体导出隔离，以及上一份可读项目快照恢复。
- 新增可视化多视频/音频轨、Command 多选、批量删除/移轨、音频拖放到指定轨道、交付预设、快捷键定制和项目诊断。
- 导出视频合成已迁移到 AVFoundation `Configuration` API，避免已弃用可变视频合成类型的编译警告。
- 新增同轨重叠式交叉叠化、多视频轨层级合成与逐剪辑基础调色、标准 3D `.cube` LUT 像素渲染；调色临时源会在预览或导出结束后清理。
- 新增项目时间线预览入口，复用导出合成、画布、音频混合、转场与调色状态，同时保留单媒体预览。
- 新增导出帧级渲染、LUT 解析、重叠剪辑和多轨层级回归测试。

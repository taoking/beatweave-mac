# Changelog

所有显著变更都会记录在此文件中。

## [Unreleased]

### Added

- 初始化 Phase 0 原生 macOS 文档应用基础。
- 版本化 `.beatweave` 包、稳定 JSON 编解码与项目包单元测试。
- 增加 GitHub Actions 构建工作流。
- 新增媒体导入、元数据、缩略图、来源状态、重连和 AVPlayer 源媒体预览。
- 新增媒体导入、来源状态与媒体库操作测试。
- 新增可持久化、可再生的多分辨率波形缓存和波形时间线视图。
- 新增 PCM 波形生成及项目包缓存往返测试。
- 新增本地 vDSP FFT 节拍分析、起音/BPM/节拍网格、手动 BPM、半/双拍与 Tap Tempo。
- 新增合成点击音频端到端分析、信号处理与节拍标记持久化测试。
- 新增可撤销的时间线命令层、视频/原始音频剪辑联动、节拍感知吸附、拖放、裁剪、分割、重排与波纹删除。
- 新增时间线插入、移动、裁剪、分割、波纹删除、吸附及撤销/重做测试。
